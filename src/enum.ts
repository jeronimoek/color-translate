export enum AngleUnit {
  NONE = "none",
  DEG = "deg",
  GRAD = "grad",
  RAD = "rad",
  TURN = "turn",
}

export enum ValueType {
  DECIMAL = "decimal",
  PERCENTAGE = "percentage",
  ANGLE = "angle",
}

export enum ColorVersion {
  LEGACY = "legacy",
  CURRENT = "current",
}
